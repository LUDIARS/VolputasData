# Survey definitions

Survey definition documents are generated by Voluptas. Each document uses OKF v0.1 Markdown with
YAML frontmatter and contains a lossless JSON representation of the questionnaire.
