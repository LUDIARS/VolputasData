# Survey responses

Responses are intentionally absent from every published branch. Volputas writes local response
JSON under `answers/<Name>/<survey-id>.json`; the repository `.gitignore` excludes those files.

Do not change the ignore rule to publish a real person's answers.
