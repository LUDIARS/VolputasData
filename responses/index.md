# Survey responses

Responses are intentionally absent from `main`. Voluptas writes each person's OKF response
document to a branch named `responses/github-<numeric-id>` and pushes that branch directly.
